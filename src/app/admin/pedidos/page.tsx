import { AdminNav } from "@/components/admin/admin-nav";
import { requireAdminUser } from "@/lib/admin";
import { formatPrice } from "@/lib/format";
import { updateOrder } from "../actions";
export default async function OrdersPage(){const {supabase}=await requireAdminUser();const {data:orders}=await supabase.from("orders").select("id,order_number,customer_name,customer_email,customer_phone,shipping_region,shipping_commune,shipping_address,total,status,payment_status,tracking_code,tracking_url,created_at,order_items(product_name,quantity)").order("created_at",{ascending:false});return <main className="admin-shell"><AdminNav active="orders"/><section className="admin-content"><header><div><span className="eyebrow">Ventas</span><h1>Pedidos</h1><p>Compras, pagos y seguimiento de despachos.</p></div></header><div className="order-admin-list">{orders?.length?orders.map(o=><article className="order-admin-card" key={o.id}><div><small>Pedido #{o.order_number}</small><h2>{o.customer_name}</h2><p>{o.customer_email} · {o.customer_phone}</p><p>{o.shipping_address}, {o.shipping_commune}, {o.shipping_region}</p><p>{o.order_items.map(i=>`${i.quantity}× ${i.product_name}`).join(" · ")}</p></div><div><strong>{formatPrice(o.total)}</strong><span className="status-active">{o.payment_status}</span><form action={updateOrder}><input type="hidden" name="id" value={o.id}/><select name="status" defaultValue={o.status}><option value="paid">Pagado</option><option value="preparing">Preparando</option><option value="shipped">Enviado</option><option value="delivered">Entregado</option><option value="cancelled">Cancelado</option></select><input name="tracking_code" placeholder="Código seguimiento" defaultValue={o.tracking_code??""}/><input name="tracking_url" type="url" placeholder="URL seguimiento" defaultValue={o.tracking_url??""}/><button>Actualizar</button></form></div></article>):<div className="admin-empty"><h2>Aún no hay pedidos</h2><p>Las compras iniciadas con Mercado Pago aparecerán aquí.</p></div>}</div></section></main>}
